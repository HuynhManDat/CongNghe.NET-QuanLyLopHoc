@model HocVien
@{
    ViewBag.Title = "Sửa học viên";
    ViewBag.Controller = "Học viên";
    ViewBag.General = "active";
    ViewBag.HocVien = "active";
    ViewBag.Id = "view-null";
}

<div class="row">
    <!-- left column -->
    <div class="col-md-12">
        <!-- general form elements -->
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">@ViewBag.Title</h3>
            </div><!-- /.box-header -->
            <!-- form start -->
            <form action="@Url.Action("Edit")" method="post" class="form-horizontal">
                @Html.AntiForgeryToken()
                @Html.Partial("_Update", Model)
                <i>Mật khẩu mặc định: 123456</i>
                <div class="box-footer">
                    <a href="@Url.Action("Index")" class="btn btn-default pull-left">Danh sách</a>
                    <button type="submit" class="btn btn-primary pull-right">Lưu</button>
                </div>
            </form>
        </div>
    </div>
</div>

